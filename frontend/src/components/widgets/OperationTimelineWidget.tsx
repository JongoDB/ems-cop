export default function OperationTimelineWidget({ id }: { id: string }) {
  return <div data-widget-id={id} className="p-4 text-gray-400 text-sm">OperationTimelineWidget — not yet implemented</div>
}
